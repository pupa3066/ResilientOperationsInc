# BUILD LOG — Day 3
**Date:** 2026-05-13  
**Project:** ResilientOperationsInc  
**Session:** Hackathon Day 3 — Edge Case Hardening

---

## 🕐 Timeline

| Time (PDT) | Activity |
|---|---|
| 09:50 | Identified 6 critical edge cases for hackathon judging |
| 09:55 | Edge case 2: False positive guard in DetectionAgent |
| 10:05 | Edge case 5: Unknown incident type — evidence-based classification |
| 10:15 | Edge case 3: HITL rejection dependency graph in ActionAgent + Planner |
| 10:25 | Edge case 1: Cascading incident detection in log_reader + agent_main |
| 10:30 | Edge case 4: New signals during remediation — re-check loop |
| 10:35 | Edge case 6: Empty/malformed log handling in log_reader |
| 10:40 | Full pipeline verified end to end |

---

## ✅ Edge Cases Fixed

### Edge Case 2 — False Positive Prevention (`agents/detection_agent.py`)

**Problem:** A single transient ERROR log triggered the full pipeline.

**Fix:**
- `MIN_SIGNALS = 5` — incident must have ≥5 error signals before firing
- `COOLDOWN_SECONDS = 60` — same source cannot re-fire within 60 seconds
- Deduplication via `_cooldowns` dict keyed by source filename

```
[DetectionAgent] ⚪ incident-1.log — 2 signals below threshold=5, suppressed (false positive guard)
[DetectionAgent] 🟡 incident-2.log — cooldown active, suppressed duplicate fire
```

---

### Edge Case 5 — Unknown Incident Type (`reasoning/reason.py`)

**Problem:** Anything not matching 3 hardcoded patterns fell through to OOM handler — wrong diagnosis.

**Fix:** Evidence-based scoring. Each pattern (db_pool, network, oom) is scored by keyword hits in log content. If no pattern scores ≥2, returns `UNKNOWN` result with raw evidence and safe investigation-only steps instead of a wrong diagnosis.

```python
scores = {
    "db_pool": sum(kw in logs_text for kw in ["HikariPool", "JDBC", "max_connections", ...]),
    "network": sum(kw in logs_text for kw in ["packet loss", "rds-proxy", "ECONNREFUSED", ...]),
    "oom":     sum(kw in logs_text for kw in ["OutOfMemoryError", "OOMKiller", "heap space", ...]),
}
if max(scores.values()) < 2:
    return UNKNOWN result  # safe fallback, no wrong action taken
```

---

### Edge Case 3 — HITL Rejection Dependency Graph (`agents/action_agent.py`, `planner/plan.py`)

**Problem:** Rejecting step 3 didn't block steps 4 and 5 that depended on it.

**Fix:** Each plan step now declares `depends_on: [step_numbers]`. ActionAgent checks this before executing — if a dependency was rejected, all downstream steps are automatically blocked.

```
[ActionAgent] 🚫 Step 4 BLOCKED — depends on rejected step 3: Increase HikariCP pool size...
[ActionAgent] 🚫 Step 5 BLOCKED — depends on rejected step 4: Deploy updated payment-svc...
[ActionAgent] ⚠️  PARTIALLY_RESOLVED — approved=1 rejected=1 blocked=2
```

Dependency map per incident:
- DB pool: `4→3`, `5→4`
- Packet loss: `4→3`, `5→3`
- OOM: `4→3`

---

### Edge Case 1 — Cascading Incidents (`observation/log_reader.py`, `agent_main.py`)

**Problem:** Two root causes firing simultaneously on shared services caused duplicate/conflicting plans.

**Fix:** `load_signals()` cross-references all incidents after loading. Any two incidents sharing a service are flagged as `cascade_candidates`. The orchestrator warns the operator before reasoning begins.

```
⚡ CASCADE WARNING: incident-1.log shares services ['api-gateway'] with incident-2.log — may be related
⚡ CASCADE WARNING: incident-1.log shares services ['api-gateway'] with incident-3.log — may be related
```

---

### Edge Case 4 — Incident During Remediation (`agent_main.py`)

**Problem:** New signals arriving while a remediation was running were ignored until the next manual run.

**Fix:** After each remediation round, `agent_main.py` re-calls `detection.run()` to check for new incidents. Already-processed sources are tracked in a `processed` set. Cooldown in DetectionAgent prevents re-firing the same incident.

```
Checking for new incidents that arrived during remediation (round 2)...
[DetectionAgent] 🟡 incident-1.log — cooldown active, suppressed duplicate fire
No new incidents detected after remediation.
```

---

### Edge Case 6 — Empty/Malformed Logs (`observation/log_reader.py`)

**Problem:** Empty log files or malformed lines caused silent crashes or zero-signal incidents.

**Fix:**
- `parse_log()` wraps file reads in try/except — unreadable files return `[]` with a warning
- Malformed lines are silently skipped (regex non-match)
- `load_signals()` skips files with zero parseable entries with an explicit warning
- If no log files exist at all, returns `[]` with a warning instead of crashing

```
[log_reader] ⚠ incident-4.log is empty or unreadable — skipping
[log_reader] ⚠ No incident log files found in .../logs
```

---

## 📁 Files Changed

| File | Change |
|---|---|
| `agents/detection_agent.py` | MIN_SIGNALS threshold + cooldown |
| `agents/action_agent.py` | depends_on dependency graph + blocked count |
| `agents/reasoning_agent.py` | docstrings |
| `reasoning/reason.py` | evidence-based scoring + UNKNOWN fallback |
| `planner/plan.py` | depends_on fields on all plan steps + unknown plan |
| `observation/log_reader.py` | empty/malformed handling + cascade detection |
| `agent_main.py` | cascade warnings + post-remediation re-check loop |

---

## 🔜 Next Up — Day 4
- Real Gemini API integration (fresh key)
- FastAPI backend — expose pipeline as REST API
- Incident report auto-generation
- Elastic MCP integration

---

## 🧠 Why These Changes Matter for the Final Product

### Why false positive prevention is critical
In production, transient errors happen constantly — a single timeout, a brief network blip, a slow query. Without a signal threshold, the AI agent would fire on every noise event, overwhelming SREs with false alarms and destroying trust in the system. The `MIN_SIGNALS=5` threshold ensures only sustained, multi-signal incidents trigger the pipeline. The 60-second cooldown prevents alert storms from the same source hammering the reasoning layer repeatedly.

### Why evidence-based classification beats hardcoded patterns
The original `else → OOM` fallback was a silent failure mode: any unknown incident would be diagnosed as an OOM kill and get an OOM remediation plan applied to it. In a real production system this could cause harm — restarting pods when the real issue is a network partition, for example. The scoring system ensures the agent only acts when it has sufficient evidence, and returns a safe "investigate manually" plan when it doesn't.

### Why the HITL dependency graph is non-negotiable
Human approval gates are the core safety mechanism of ResilienceOps. But approval gates without dependency enforcement create a false sense of safety: an SRE rejects a dangerous step, but the system executes dependent steps anyway because it doesn't know they're related. The `depends_on` graph makes the safety model complete — a rejection propagates forward through the plan automatically.

### Why cascade detection matters at scale
In microservice architectures, a single root cause (e.g. network packet loss) manifests as errors across many services simultaneously. Without cascade detection, the system generates N independent remediation plans for what is actually one incident — potentially applying conflicting fixes. Flagging shared services as cascade candidates lets the SRE treat related incidents as one coordinated response.

### Why post-remediation re-check is essential for real-time operation
The pipeline was originally a one-shot batch process. In real incidents, new failures arrive continuously — a DB fix might reveal a downstream service that was masked by the original error. The re-check loop turns ResilienceOps from a batch tool into a continuous incident commander that stays active until the system is fully stable.

### Why empty/malformed log handling matters for demos and production
A crash on missing or malformed input is the most embarrassing failure mode in a hackathon demo. More importantly, in production, log pipelines fail, files get truncated, and encoding issues happen. Graceful degradation with clear warnings is the difference between a system that helps during an incident and one that adds to the chaos.
